{
  "name": "porter-md-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "description": "porter.md — MediaPorter marketing site",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist --project-name=porter-md"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
