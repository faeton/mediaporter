__pycache__/
*.pyc
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/
.ruff_cache/
*.m4v
*.mp4
*.mkv
*.avi
test_fixtures/
.env
.DS_Store
traces/
python-reference/scripts/test_*.py
*.sqlitedb
.venv/
.claude/
raw/

# Raw (un-masked) sync authentication seed. Do not commit captured blobs.
# The bundled `SyncAuthSeed.dat` files in Resources/data dirs are XOR-masked
# and *are* committed.
MacApp/Private/
MacApp/MediaPorter/Resources/grappa.bin
python-reference/src/mediaporter/sync/data/grappa.bin

# Personal experiments / probe scripts / test-corpus generators. Anything
# in a `local/` directory under scripts is considered local-only — handy
# for one-off diagnostics and test-asset generators that shouldn't ship
# with the repo.
scripts/local/
**/scripts/local/

# site/ (Astro + Cloudflare Pages)
site/node_modules/
site/dist/
site/.astro/
site/.wrangler/
site/.dev.vars

# codanna code-intelligence index (machine-specific abs paths + large index; regenerate with `codanna index`)
# .codannaignore is committed (ignore rules travel with the repo); .codanna/ is not.
.codanna/
.fastembed_cache
